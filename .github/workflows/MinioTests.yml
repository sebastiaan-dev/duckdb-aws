name: Minio Tests
on: [push, pull_request,repository_dispatch]
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' || github.sha }}
  cancel-in-progress: true
defaults:
  run:
    shell: bash

jobs:
  minio-tests:
    name: Minio Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        duckdb_version: [ '<submodule_version>' ]
    env:
      S3_TEST_SERVER_AVAILABLE: 1
      DUCKDB_S3_ENDPOINT: duckdb-minio.com:9000
      DUCKDB_S3_USE_SSL: false
      GEN: ninja
      VCPKG_TARGET_TRIPLET: x64-linux
      VCPKG_TOOLCHAIN_PATH: ${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
      DONT_STATIC_LINK_DUCKDB: 1 # fixes oom while linking

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: 'true'

      - name: Checkout DuckDB to version
        if: ${{ matrix.duckdb_version != '<submodule_version>'}}
        run: |
          cd duckdb
          git checkout ${{ matrix.duckdb_version }}

      - uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install Ninja
        shell: bash
        run: sudo apt-get update -y -qq && sudo apt-get install -y -qq ninja-build

      - name: Setup Ccache
        uses: hendrikmuhs/ccache-action@main
        with:
          key: ${{ github.job }}
          save: ${{ github.ref == 'refs/heads/main' || github.repository != 'duckdb/duckdb' }}

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11.1
        with:
          vcpkgGitCommitId: 84bab45d415d22042bd0b9081aea57f362da3f35

      - name: Build
        shell: bash
        run: make

      - name: Start S3/HTTP test server
        shell: bash
        run: |
          ./scripts/install_and_run_httpfs_s3_test_server.sh

      - name: Write AWS credentials file
        shell: bash
        run: |
          ./scripts/create_minio_credential_file.sh

      - name: Test
        shell: bash
        run: |
          export AWS_CONFIG_FILE="$HOME/.aws/config";
          export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials";
          make test
      - name: Prepare AWS secret bug reproducers
        shell: bash
        run: |
          ./scripts/setup_aws_secret_bug_repro.sh "$RUNNER_TEMP/aws-secret-bug-repro"

      - name: Start local STS test server
        shell: bash
        run: |
          python3 ./scripts/aws_secret_sts_test_server.py >"$RUNNER_TEMP/aws-secret-sts.log" 2>&1 &
          echo "$!" >"$RUNNER_TEMP/aws-secret-sts.pid"
          sleep 1

      - name: Run AWS secret bug reproducers
        shell: bash
        env:
          DUCKDB_AWS_SECRET_BUG_REPRO: 1
          AWS_CONFIG_FILE: ${{ runner.temp }}/aws-secret-bug-repro/config
          AWS_SHARED_CREDENTIALS_FILE: ${{ runner.temp }}/aws-secret-bug-repro/credentials
          AWS_PROFILE: wrong-repro
          AWS_ENDPOINT_URL_STS: http://127.0.0.1:8898
          AWS_EC2_METADATA_DISABLED: 'true'
        run: |
          for test in \
            test/sql/env/aws_secret_bug_source_profile.test \
            test/sql/env/aws_secret_bug_config_role_metadata.test
          do
            ./build/release/test/unittest "$test"
          done

      - name: Run Env tests
        shell: bash
        env:
          DUCKDB_AWS_TESTING_ENV_AVAILABLE: 1
          AWS_ACCESS_KEY_ID: duckdb_env_testing_id
          AWS_SECRET_ACCESS_KEY: duckdb_env_testing_key
          AWS_DEFAULT_REGION: duckdb_env_testing_region
        run: |
          export AWS_CONFIG_FILE="$HOME/.aws/config";
          export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials";
          ./build/release/test/unittest "*/test/sql/env/*"

      - name: Run AWS_PROFILE env tests
        shell: bash
        env:
          DUCKDB_AWS_TESTING_ENV_AVAILABLE: 1
          AWS_PROFILE: minio-testing-2
        run: |
          export AWS_CONFIG_FILE="$HOME/.aws/config";
          export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials";
          ./build/release/test/unittest test/sql/env/aws_secret_sso_profile_env.test

      - name: Run container credentials env tests
        shell: bash
        env:
          AWS_CONTAINER_CREDENTIALS_FULL_URI: http://127.0.0.1:8899/creds
          AWS_CONTAINER_AUTHORIZATION_TOKEN: duckdb-container-test-token
        run: |
          python3 ./scripts/run_container_credentials_test_server.py 8899 duckdb-container-test-token &
          sleep 1
          ./build/release/test/unittest test/sql/env/aws_secret_container_env.test

      - name: Run mitmproxy
        shell: bash
        run: |
          python -m venv .venv
          source .venv/bin/activate
          python3 -m pip install mitmproxy==12.2.1
          mitmdump --mode regular@8080 --flow-detail 2 > mitmproxy.log 2>&1 &
          # sleep otherwise proxy tests fail
          sleep 30

      - name: Run proxy tests
        shell: bash
        env:
          HTTP_PROXY_PUBLIC: localhost:8080
        run: |
          export AWS_CONFIG_FILE="$HOME/.aws/config";
          export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials";
          ./build/release/test/unittest test/sql/env/proxy/aws_secret_http_proxy.test

      - name: Run mitmproxy with username and password
        shell: bash
        run: |
          pkill -f "mitmdump" || true
          source .venv/bin/activate
          mitmdump --mode regular@8080 --proxyauth admin:password --flow-detail 2 > mitmproxy.log 2>&1 &

      - name: Run proxy tests with auth
        shell: bash
        env:
          HTTP_PROXY_PUBLIC: localhost:8080
        run: |
          export AWS_CONFIG_FILE="$HOME/.aws/config";
          export AWS_SHARED_CREDENTIALS_FILE="$HOME/.aws/credentials";
          ./build/release/test/unittest test/sql/env/proxy/aws_secret_http_proxy_with_auth.test
